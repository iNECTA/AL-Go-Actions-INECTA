name: PowerShell Script
author: Vivek Jindal
runs:
  using: composite
  steps:
    - run: try { ${{ github.action_path }}/BranchCutOff.ps1 catch { Write-Host "::Error::Unexpected error when running action ($($_.Exception.Message))"; exit 1 }
      shell: PowerShell
branding:
  icon: terminal
  color: blue
